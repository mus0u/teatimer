teatimer.site: the site that times your tea.

written in no-frills, library-free javascript, and plain old html and css.

MIT license.

if you have ruby installed, you can run `./test-server.rb` from the project root
to serve the files on `localhost:3000`.
